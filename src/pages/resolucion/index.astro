---
import { getCollection } from "astro:content"
import PageLayout from "@layouts/PageLayout.astro"
import TopLayout from "@layouts/TopLayout.astro"
import BottomLayout from "@layouts/BottomLayout.astro"
import { WORK } from "@consts"

const collection = await getCollection("work")

collection.sort((a, b) => new Date(b.data.dateStart).getTime() - new Date(a.data.dateStart).getTime())

function formatWorkDate(input: Date | string) {
  if (typeof input === "string") return input

  const month = input.toLocaleDateString("es-ES", {
    month: "short",
  })

  const year = new Date(input).getFullYear()
  return `${month} ${year}`
}
---
<div class="top-0 fixed -z-99999 opacity-5">
  <video
  src="https://www.rpg.es/wp-content/uploads/2024/03/1-Cabecera_Web_FullHD_low.mp4"
  >
</video>
</div>
<PageLayout title={WORK.TITLE} description={WORK.DESCRIPTION}>
  <TopLayout>
    <div class="flex justify-between animate page-heading">
      <a
      href="/work"
      type="button" 
      class="flex w-20 justify-center text-xs font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
      <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icon-tabler-arrow-back" width="28" height="28" viewBox="0 0 24 24" stroke-width="1.5" stroke="#ffffff" fill="none" stroke-linecap="round" stroke-linejoin="round">
        <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
        <path d="M9 11l-4 4l4 4m-4 -4h11a4 4 0 0 0 0 -8h-1" />
      </svg>
      </a>

      <a 
      href="#" 
      class="flex w-20 justify-center text-xs font-medium text-center text-white bg-blue-700 rounded-lg hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 dark:bg-blue-600 dark:hover:bg-blue-700 dark:focus:ring-blue-800">
        <svg xmlns="http://www.w3.org/2000/svg" class="icon icon-tabler icon-tabler-printer" width="28" height="28" viewBox="0 0 24 24" stroke-width="1.5" stroke="#ffffff" fill="none" stroke-linecap="round" stroke-linejoin="round">
            <path stroke="none" d="M0 0h24v24H0z" fill="none"/>
            <path d="M17 17h2a2 2 0 0 0 2 -2v-4a2 2 0 0 0 -2 -2h-14a2 2 0 0 0 -2 2v4a2 2 0 0 0 2 2h2" />
            <path d="M17 9v-4a2 2 0 0 0 -2 -2h-6a2 2 0 0 0 -2 2v4" />
            <path d="M7 13m0 2a2 2 0 0 1 2 -2h6a2 2 0 0 1 2 2v4a2 2 0 0 1 -2 2h-6a2 2 0 0 1 -2 -2z" />
          </svg>
    </a>
</header>

</div>
</TopLayout>
<BottomLayout>
  <div class="flex justify-center mb-4">
    <p class="font-medium mt-4 text-balance">
      La pantalla se compone de un total de <span class="text-[#09f]">208</span> cabinets, cada uno con una resolución de <span class="text-[#09f]">384x216</span> píxeles.<br>
      Cada cabinet, a su vez, está formado por <span class="text-[#09f]">4 módulos,</span> cada uno con una resolución de <span class="text-[#09f]">192x108</span> píxeles.
      <br>La pantalla se organiza en <span class="text-[#09f]">16</span> columnas de cabinets por <span class="text-[#09f]">13</span> filas, lo que proporciona una resolución total de <span class="text-[#09f]">6144x5616</span> píxeles.<br> Esta resolución total se divide en <span class="text-[#09f]">dos secciones</span>, donde cada sección de la pantalla ofrece una resolución de <span class="text-[#09f]">3072x2808</span> píxeles para cada mitad.
    </p>
  </div>
  <div id="img" class="flex justify-center">
    <img src="res.png" alt="estructura">
  </div>
  </BottomLayout>
</PageLayout>
